# Checkpoint de reescritura editorial

Estado: en progreso. Todos los perfiles permanecen en `pending`.

Perfiles reescritos manualmente, con investigación externa y sin usar `biography`:

- `alfred-hitchcock`
- `aaron-taylor-johnson`
- `mercedes-moran`
- `meryl-streep`
- `robert-de-niro`
- `adam-driver` a `ben-kingsley` (30 perfiles, en orden de catálogo)
- `benedict-cumberbatch` a `chris-pine` (30 perfiles, en orden de catálogo)
- `chris-pratt` a `david-arquette` (30 perfiles, en orden de catálogo)
- `david-fincher` a `ewan-mcgregor` (30 perfiles, en orden de catálogo)
- `f-murray-abraham` a `helena-bonham-carter` (30 perfiles, en orden de catálogo)
- `henry-cavill` a `jeremy-strong` (30 perfiles, en orden de catálogo)
- `jesse-eisenberg` a `julianne-moore` (30 perfiles, en orden de catálogo)
- `julieta-diaz` a `mahershala-ali` (30 perfiles, en orden de catálogo)
- `margaret-qualley` a `morgan-freeman` (30 perfiles, en orden de catálogo)
- `nahuel-perez-biscayart` a `rafael-ferro` (30 perfiles, en orden de catálogo)
- `ralph-fiennes` a `samuel-l-jackson` (30 perfiles, en orden de catálogo)

Las once primeras oleadas quedaron incorporadas el 2026-07-12. Próximo perfil: `sandra-huller`.

No aprobar ni indexar ningún perfil hasta que el catálogo completo pase la auditoría factual, de fuentes y de similitud.
